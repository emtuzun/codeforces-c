#include <stdio.h>
#include <string.h>

int main()
{
  int n;
  scanf("%d", &n);
  int a[n], b[n];
  for(int i = 0; i<n; i++){
    scanf("%d %d", a[i], b[i]);
  }
  char result[] = "YES"; 
  printf("%s\n", result);
  return 0;
}
